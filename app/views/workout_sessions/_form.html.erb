<%= form_for([workout, workout_session]) do |f| %>
  <div class="field">
    <%= f.label :sets %><br>
    <%= f.text_field :sets, autofocus: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
